# ===== WISH =====
wish.message = Happy Birthday Rajat..! 
wish.display.time = 2

# ===== FIREWORK SPAWN RATE (per second) ===== 
firework.from = 1
firework.to = 5

# ===== EXIT CONDITIONS =====
firework.total.count = 200
firework.total.time = 60

# ===== VISUALS =====
firework.color.mode = true
firework.max.radius = 5
firework.frame.delay = 60      # ms between animation frames

# ===== PARTICLE DENSITY =====
firework.particles = 48        # points per ring (higher = smoother circle)

# ===== TAIL (rocket launch) =====
firework.tail.enabled = true
firework.tail.speed = 40       # ms per tail step (smaller = faster)
firework.tail.length = 12      # length of tail drawn

# ===== TERMINAL OVERRIDES (0 = auto detect fallback) =====
terminal.rows = 0
terminal.cols = 0

terminal.font.size = 32